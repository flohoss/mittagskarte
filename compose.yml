services:
  mittagstisch:
    build:
      context: .
      dockerfile: docker/dev.dockerfile
    platform: linux/amd64
    volumes:
      - .:/app/
    ports:
      - 7331:7331

  tailwind:
    image: node:24-alpine
    working_dir: /app
    command: yarn dev
    tty: true
    volumes:
      - .:/app/

  yarn:
    profiles:
      - dev
    image: node:24-alpine
    working_dir: /app
    entrypoint: yarn
    volumes:
      - .:/app/

  go:
    profiles:
      - dev
    image: golang:1.25-alpine
    working_dir: /app
    entrypoint: go
    volumes:
      - .:/app/
